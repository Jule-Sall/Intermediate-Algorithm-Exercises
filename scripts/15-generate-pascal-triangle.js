/*const outerTriangle = [];
const n = 5;
let row = 0;
let col = n - 1;
outerTriangle[row][col] = 1;
let iterator = 1;
while (iterator < n) {
    row = row + 1;
    col = col + iterator;
    outerTriangle[row][col] = 1;
    col = col - iterator;
    outerTriangle[row][col] = 1;
    iterator++;
}

for (let i = 0; i < 10; i++) {
    for (let j = 0; j < 10; j++) {
        if (outerTriangle[i][j])
            console.log(outerTriangle);
    }
}
    */
   // work in progess
